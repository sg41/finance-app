name: finapp
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  # flutter_secure_storage: ^9.2.2  <-- УДАЛИТЕ ИЛИ ЗАКОММЕНТИРУЙТЕ
  shared_preferences: ^2.2.3 # <-- ДОБАВЬТЕ ЭТОТ ПАКЕТ
  provider: ^6.1.2
  flutter_svg: ^2.0.10+1 # <-- ДОБАВЬТЕ ЭТУ СТРОКУ

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
